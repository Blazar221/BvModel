export class BvNode {
  constructor(id, index, x, y, z, hint, type, size, flashSpeed) {
    this.id = id
    this.index = index
    this.x = x
    this.y = y
    this.z = z
    this.hint = hint
    this.type = type
    this.size = size
    this.flashSpeed = flashSpeed
  }
}
